...state,
                {
                   name: action.name,
                   id: action.id,
                   price: action.price,
                   inStock: action.inStock,
                   photoUrl: action.photoUrl
                }


                <Counter id={products.id}/>